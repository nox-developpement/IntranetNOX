{% extends "::layoutV2.html.twig" %}

{% block titrePage %}
    Modif Entité
{% endblock %}

{% block messageAccueil %}Administration Entité{% endblock %}


{% block contenu %}
    
    <style>
        div.col-sm-8{
            margin-bottom: 10px;
        }
    </style>  
    
<div class="box box-primary">
    <div class="box-header with-border">
      <h3 class="box-title"></h3>
    </div>

    {{ form_start(form) }}
    <div class="box-body col-sm-offset-2">

        {# Les erreurs générales du formulaire. #}
        {{ form_errors(form) }}

        {# Génération manuelle et éclatée : #}
        <div class="form-group">
          {# Génération du label. #}
          {{ form_label(form.societe, "Societe", {'label_attr': {'class': 'col-sm-12 control-label'}}) }}
          {# Affichage des erreurs pour ce champ précis. #}
          {{ form_errors(form.societe) }}
          <div class="col-sm-8">
            {# Génération de l'input. #}
            {{ form_widget(form.societe, {'attr': {'class': 'form-control'}}) }}
          </div>
        </div>
        <div class="form-group">
          {# Génération du label. #}
          {{ form_label(form.etablissement, "Etablissement", {'label_attr': {'class': 'col-sm-12 control-label'}}) }}
          {# Affichage des erreurs pour ce champ précis. #}
          {{ form_errors(form.etablissement) }}
          <div class="col-sm-8">
            {# Génération de l'input. #}
            {{ form_widget(form.etablissement, {'attr': {'class': 'form-control'}}) }}
          </div>
        </div>
        <div class="form-group">
          {# Génération du label. #}
          {{ form_label(form.aa, "AA", {'label_attr': {'class': 'col-sm-12 control-label'}}) }}
          {# Affichage des erreurs pour ce champ précis. #}
          {{ form_errors(form.aa) }}
          <div class="col-sm-8">
            {# Génération de l'input. #}
            {{ form_widget(form.aa, {'attr': {'class': 'form-control'}}) }}
          </div>
        </div>
        <div class="form-group">
          {# Génération du label. #}
          {{ form_label(form.da, "DA", {'label_attr': {'class': 'col-sm-12 control-label'}}) }}
          {# Affichage des erreurs pour ce champ précis. #}
          {{ form_errors(form.da) }}
          <div class="col-sm-8">
            {# Génération de l'input. #}
            {{ form_widget(form.da, {'attr': {'class': 'form-control'}}) }}
          </div>
        </div>
        <div class="form-group">
          {# Génération du label. #}
          {{ form_label(form.arh, "ARH", {'label_attr': {'class': 'col-sm-12 control-label'}}) }}
          {# Affichage des erreurs pour ce champ précis. #}
          {{ form_errors(form.arh) }}
          <div class="col-sm-8">
            {# Génération de l'input. #}
            {{ form_widget(form.arh, {'attr': {'class': 'form-control'}}) }}
          </div>
        </div>
        <div class="form-group">
          {# Génération du label. #}
          {{ form_label(form.n2, "N+2", {'label_attr': {'class': 'col-sm-12 control-label'}}) }}
          {# Affichage des erreurs pour ce champ précis. #}
          {{ form_errors(form.n2) }}
          <div class="col-sm-8">
            {# Génération de l'input. #}
            {{ form_widget(form.n2, {'attr': {'class': 'form-control'}}) }}
          </div>
        </div>

    </div>
    <div class="box-footer">
        {# Pour le bouton, pas de label ni d'erreur, on affiche juste le widget #}
        {{ form_widget(form.modifer, {'attr': {'class': 'btn btn-primary'}}) }}  
    </div> 
      {# Fermeture de la balise <form> du formulaire HTML #}
    {{ form_end(form) }}

</div>

                
                
                
    
{% endblock %}