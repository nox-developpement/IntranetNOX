{% extends "::layout.html.twig" %}

{% block titrePage %}
    Monitoring des scripts planifiés
{% endblock %}

{% block messageAccueil %}Monitoring des scripts planifiés{% endblock %}

{% block contenu %}
    <style>
        form[name='addScript'] {
            display: inline-block;
            width: auto;
            padding: 0;           
            border: none;
        }

        form[name='addScript'] fieldset {
            margin: 0;
            padding-top: 0;
            padding-bottom: 0;
        }

        #formDiv {
            text-align: center;
        }

        #monitoringTable {
            width: 70%;
            margin: auto;
            border-collapse: collapse;         
            text-align: center;
            margin-bottom: 2em;
        }

        #monitoringTable, #monitoringTable tr, #monitoringTable th, #monitoringTable td {
            border: 1px solid black;
        }

        #monitoringTable th {
            background-color: #1F4E79;
            color: white;
        }

        .led-green {
            margin: 0 auto;
            width: 24px;
            height: 24px;
            background-color: #ABFF00;
            border-radius: 50%;
            box-shadow: rgba(0, 0, 0, 0.2) 0 -1px 7px 1px, inset #304701 0 -1px 9px, #89FF00 0 2px 12px;
        }
    </style>

    <table id='monitoringTable'>
        <tr>
            <th>Nom du script</th>
            <th>Fréquence d'itération</th>
            <th>Statut</th>
        </tr>
        {% for script in monitoredScripts %}
            <tr>
                <td>
                    {{ script.ScriptName }}
                </td>
                <td>
                    Toutes les {{ script.IterationTime }} heure(s)
                </td>
                {% if script.Statut['Statut'] %}
                    <td>
                        Actif <div class='led-green'></div>
                    </td>
                {% else %}
                    <td style='background-color: red; color: white;'>
                        Erreur<br />
                        (Dernière éxecution le {{ script.Statut['lastIteration']|date('d/m/Y à H:i:s') }})
                    </td>
                {% endif %}
            </tr>
        {% endfor %}
    </table>

    <div id="formDiv">
        {{ form_start(formAddScript) }}
        <fieldset>
            <legend>Ajouter un monitoring de script</legend>

            <p>{{ form_label(formAddScript.scriptName) }} : {{ form_widget(formAddScript.scriptName) }}</p>
            <p>{{ form_label(formAddScript.iterationTime) }} : {{ form_widget(formAddScript.iterationTime) }} heure(s)</p>
            <p>{{ form_widget(formAddScript.add) }}</p>

        </fieldset>
        {{ form_end(formAddScript) }}
    </div>
{% endblock %}