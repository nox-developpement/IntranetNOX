{% extends "::layoutV2.html.twig" %}

{% block titrePage %}
    Création Entité
{% endblock %}

{% block messageAccueil %}Demande de Recrutement{% endblock %}


{% block contenu %}
    
    <style>
        div.col-sm-8{
            margin-bottom: 10px;
        }
        textarea{
            resize: vertical;
        }
    </style>  
    
<div class="box box-primary">
    <div class="box-header with-border">
      <h3 class="box-title">Création d'une nouvelle entite</h3>
    </div>


    
    {{ form_start(form) }}
    <div class="box-body col-sm-offset-2">


        {# Les erreurs générales du formulaire. #}
        {{ form_errors(form) }}

        {# Génération manuelle et éclatée : #}
        <div class="form-group">
          {# Génération du label. #}
          {{ form_label(form.titre, "Titre", {'label_attr': {'class': 'col-sm-12 control-label'}}) }}
          {# Affichage des erreurs pour ce champ précis. #}
          {{ form_errors(form.titre) }}
          <div class="col-sm-8">
            {# Génération de l'input. #}
            {{ form_widget(form.titre, {'attr': {'class': 'form-control'}}) }}
          </div>
        </div>
        <div class="form-group">
          {# Génération du label. #}
          {{ form_label(form.reference, "Reference", {'label_attr': {'class': 'col-sm-12 control-label'}}) }}
          {# Affichage des erreurs pour ce champ précis. #}
          {{ form_errors(form.reference) }}
          <div class="col-sm-8">
            {# Génération de l'input. #}
            {{ form_widget(form.reference, {'attr': {'class': 'form-control'}}) }}
          </div>
        </div>
        <div class="form-group">
          {# Génération du label. #}
          {{ form_label(form.ville, "Ville", {'label_attr': {'class': 'col-sm-12 control-label'}}) }}
          {# Affichage des erreurs pour ce champ précis. #}
          {{ form_errors(form.ville) }}
          <div class="col-sm-8">
            {# Génération de l'input. #}
            {{ form_widget(form.ville, {'attr': {'class': 'form-control'}}) }}
          </div>
        </div>
        <div class="form-group">
          {# Génération du label. #}
          {{ form_label(form.societe, "Societe", {'label_attr': {'class': 'col-sm-12 control-label'}}) }}
          {# Affichage des erreurs pour ce champ précis. #}
          {{ form_errors(form.societe) }}
          <div class="col-sm-8">
            {# Génération de l'input. #}
            {{ form_widget(form.societe, {'attr': {'class': 'form-control'}}) }}
          </div>
        </div>
        <div class="form-group">
          {# Génération du label. #}
          {{ form_label(form.etablissement, "Etablissement", {'label_attr': {'class': 'col-sm-12 control-label'}}) }}
          {# Affichage des erreurs pour ce champ précis. #}
          {{ form_errors(form.etablissement) }}
          <div class="col-sm-8">
            {# Génération de l'input. #}
            {{ form_widget(form.etablissement, {'attr': {'class': 'form-control'}}) }}
          </div>
        </div>
        <div class="form-group">
          {# Génération du label. #}
          {{ form_label(form.contrat, "Contrat", {'label_attr': {'class': 'col-sm-12 control-label'}}) }}
          {# Affichage des erreurs pour ce champ précis. #}
          {{ form_errors(form.contrat) }}
          <div class="col-sm-8">
            {# Génération de l'input. #}
            {{ form_widget(form.contrat, {'attr': {'class': 'form-control'}}) }}
          </div>
        </div>
        <div class="form-group">
          {# Génération du label. #}
          {{ form_label(form.embauche, "Date D'embauche", {'label_attr': {'class': 'col-sm-12 control-label'}}) }}
          {# Affichage des erreurs pour ce champ précis. #}
          {{ form_errors(form.embauche) }}
          <div class="col-sm-8">
            {# Génération de l'input. #}
            {{ form_widget(form.embauche, {'attr': {'class': 'form-control'}}) }}
          </div>
        </div>
        <div class="form-group">
          {# Génération du label. #}
          {{ form_label(form.commentaire, "Commentaire", {'label_attr': {'class': 'col-sm-12 control-label'}}) }}
          {# Affichage des erreurs pour ce champ précis. #}
          {{ form_errors(form.commentaire) }}
          <div class="col-sm-8">
            {# Génération de l'input. #}
            {{ form_widget(form.commentaire, {'attr': {'class': 'form-control'}}) }}
          </div>
        </div>
          
                  

    <div class="box-footer col-sm-8">
        {# Pour le bouton, pas de label ni d'erreur, on affiche juste le widget #}
        {{ form_widget(form.ajout, {'attr': {'class': 'btn btn-primary'}}) }}  
    </div> 
      {# Fermeture de la balise <form> du formulaire HTML #}
    {{ form_end(form) }}

</div>

                
                
                
    
{% endblock %}