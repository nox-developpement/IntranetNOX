{% if app.user != null %}
    <div class="row">
        <div id="profil_picture" class="col-lg-4 col-lg-offset-4">
            <img class="img-responsive" src="{{ asset('bundles/noxintranetuser/images/User-128-hover.png') }}" alt='Profi picture' onmousedown="return false;">
        </div>
        <div id="profil_username" class="col-lg-12 text-center text-white">{{ app.user.firstname }} {{ app.user.lastname }}</div>
        <a class="col-lg-12 text-center link-light-blue" href="{{ path('logout') }}">Déconnexion</a> 
    </div>
{% else %}
    <div id='login' class="row">
        <form method="POST" action="{{ path("AD_connexion_check") }}" name='connexion' class="col-lg-12" data-toggle="tooltip" title="Utilisez les mêmes identifiants que pour votre session Windows.\n ex: Vous êtes 'Henry Martin', votre login est 'h.martin'.">
            <div class="form-group">
                <input type='text' id='username' name='username' placeholder="Nom d'utilisateur" class="form-control">
                <input type='password' id='password'  name='password' placeholder="Mot de passe" class="form-control">
            </div>
            <input class="boutonFormulaireInverse btn btn-default text-center" type='submit' value='Connexion'>
        </form>
    </div>
{% endif %}

<script>
    $(document).ready(function () {
        $('#login').tooltip();
    });
</script>
