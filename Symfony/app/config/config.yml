imports:
    - { resource: parameters.yml }
    - { resource: security.yml }
    - { resource: services.yml }

# Put parameters here that don't need to change on each machine where the app is deployed
# http://symfony.com/doc/current/best_practices/configuration.html#application-related-configuration
parameters:
    locale: fr

framework:
    #esi:             ~
    translator:      { fallbacks: ["%locale%"] }
    secret:          "%secret%"
    router:
        resource: "%kernel.root_dir%/config/routing.yml"
        strict_requirements: ~
    form:            ~
    csrf_protection: ~
    validation:      { enable_annotations: true }
    #serializer:      { enable_annotations: true }
    templating:
        engines: ['twig']
        #assets_version: SomeVersionScheme
    default_locale:  "%locale%"
    trusted_hosts:   ~
    trusted_proxies: ~
    session:
        # handler_id set to null will use default session handler from php.ini
        handler_id:  ~
    fragments:       ~
    http_method_override: true

# Twig Configuration
twig:
    debug:            "%kernel.debug%"
    strict_variables: "%kernel.debug%"

# Assetic Configuration
assetic:
    debug:          "%kernel.debug%"
    use_controller: false
    bundles:        [ ]
    #java: /usr/bin/java
    filters:
        cssrewrite: ~
        #closure:
        #    jar: "%kernel.root_dir%/Resources/java/compiler.jar"
        #yui_css:
        #    jar: "%kernel.root_dir%/Resources/java/yuicompressor-2.4.7.jar"

# Doctrine Configuration
doctrine:
    dbal:
        driver:   pdo_mysql
        host:     "%database_host%"
        port:     "%database_port%"
        dbname:   "%database_name%"
        user:     "%database_user%"
        password: "%database_password%"
        charset:  UTF8
        # if using pdo_sqlite as your database driver:
        #   1. add the path in parameters.yml
        #     e.g. database_path: "%kernel.root_dir%/data/data.db3"
        #   2. Uncomment database_path in parameters.yml.dist
        #   3. Uncomment next line:
        #     path:     "%database_path%"

    orm:
        auto_generate_proxy_classes: "%kernel.debug%"
        naming_strategy: doctrine.orm.naming_strategy.underscore
        auto_mapping: true

# Swiftmailer Configuration
swiftmailer:
    transport: "%mailer_transport%"
    host:      "%mailer_host%"
    username:  "%mailer_user%"
    password:  "%mailer_password%"
    spool:     { type: memory }
    
fm_elfinder:
    instances:
        ckeditor:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/ImagesPublication
                        upload_allow:     ['image/png', 'image/jpg', 'image/jpeg', 'image/gif', 'application/zip', 'audio/mpeg', 'text/csv', 'video/mp4', 'video/webm', 'application/pdf']
                        upload_deny:      ['all']
                        upload_max_size:  10000M
        archives:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Archives
                        upload_allow:     ['image/png', 'image/jpg', 'image/jpeg', 'image/gif', 'application/zip', 'audio/mpeg', 'text/csv', 'video/mp4', 'video/webm', 'application/pdf']
                        upload_deny:      ['all']
                        upload_max_size:  100M
        competences:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Competences
                        upload_allow:     ['application/pdf']
                        upload_deny:      ['all']
                        upload_max_size:  100M
        newsSI:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/SI/NewsSI
                        upload_allow:     ['application/pdf']
                        upload_deny:      ['all']
                        upload_max_size:  100M
        noxNews:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Interne/NoxNews
                        upload_allow:     ['application/pdf']
                        upload_deny:      ['all']
                        upload_max_size:  100M
        noxLetters:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Externe/NoxLetters
                        upload_allow:     ['application/pdf']
                        upload_deny:      ['all']
                        upload_max_size:  100M
        references:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/References
                        upload_allow:     ['image/png', 'image/jpg', 'image/jpeg', 'image/gif', 'application/zip', 'audio/mpeg', 'text/csv', 'video/mp4', 'video/webm', 'application/pdf']
                        upload_deny:      ['all']
                        upload_max_size:  100M
        plaquettesInstitutionnelle:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Externe/Supports/PlaquettesInstitutionnelle
                        upload_allow:     ['image/png', 'image/jpg', 'image/jpeg', 'image/gif', 'application/zip', 'audio/mpeg', 'text/csv', 'video/mp4', 'video/webm', 'application/pdf']
                        upload_deny:      ['all']
                        upload_max_size:  100M
        presentationPowerPoint:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Externe/Supports/PresentationPowerpoint
                        upload_allow:     ['image/png', 'image/jpg', 'image/jpeg', 'image/gif', 'application/zip', 'audio/mpeg', 'text/csv', 'video/mp4', 'video/webm', 'application/pdf']
                        upload_deny:      ['all']
                        upload_max_size:  100M
        fichesMetier:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Externe/Supports/FichesMetier
                        upload_allow:     ['image/png', 'image/jpg', 'image/jpeg', 'image/gif', 'application/zip', 'audio/mpeg', 'text/csv', 'video/mp4', 'video/webm', 'application/pdf']
                        upload_deny:      ['all']
                        upload_max_size:  100M
        dossierTypeAppelOffre:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Externe/Supports/DossierTypeAppelOffre
                        upload_allow:     ['image/png', 'image/jpg', 'image/jpeg', 'image/gif', 'application/zip', 'audio/mpeg', 'text/csv', 'video/mp4', 'video/webm', 'application/pdf']
                        upload_deny:      ['all']
                        upload_max_size:  100M
        CVType:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Externe/Supports/CVType
                        upload_allow:     ['image/png', 'image/jpg', 'image/jpeg', 'image/gif', 'application/zip', 'audio/mpeg', 'text/csv', 'video/mp4', 'video/webm', 'application/pdf']
                        upload_deny:      ['all']
                        upload_max_size:  100M
        courriersType:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Externe/Supports/CourriersType
                        upload_allow:     ['image/png', 'image/jpg', 'image/jpeg', 'image/gif', 'application/zip', 'audio/mpeg', 'text/csv', 'video/mp4', 'video/webm', 'application/pdf']
                        upload_deny:      ['all']
                        upload_max_size:  100M
        papierEntete:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Externe/Supports/PapierEntete
                        upload_allow:     ['image/png', 'image/jpg', 'image/jpeg', 'image/gif', 'application/zip', 'audio/mpeg', 'text/csv', 'video/mp4', 'video/webm', 'application/pdf']
                        upload_deny:      ['all']
                        upload_max_size:  100M
        noxDansLaPresse:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Externe/NoxDansLaPresse
                        upload_allow:     ['image/png', 'image/jpg', 'image/jpeg', 'image/gif', 'application/zip', 'audio/mpeg', 'text/csv', 'video/mp4', 'video/webm', 'application/pdf']
                        upload_deny:      ['all']
                        upload_max_size:  100M
        logoNox:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Externe/BanqueImages/LogoNox
                        upload_allow:     ['image/png', 'image/jpg', 'image/jpeg', 'image/gif', 'application/zip', 'audio/mpeg', 'text/csv', 'video/mp4', 'video/webm', 'application/pdf']
                        upload_deny:      ['all']
                        upload_max_size:  100M
        cellesDuSiteInternet:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Externe/BanqueImages/CellesDuSiteInternet
                        upload_allow:     ['image/png', 'image/jpg', 'image/jpeg', 'image/gif', 'application/zip', 'audio/mpeg', 'text/csv', 'video/mp4', 'video/webm', 'application/pdf']
                        upload_deny:      ['all']
                        upload_max_size:  100M
        cellesDesSupports:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Externe/BanqueImages/CellesDesSupports
                        upload_allow:     ['image/png', 'image/jpg', 'image/jpeg', 'image/gif', 'application/zip', 'audio/mpeg', 'text/csv', 'video/mp4', 'video/webm', 'application/pdf']
                        upload_deny:      ['all']
                        upload_max_size:  100M
        trombinoscope:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Interne/Trombinoscope
                        upload_allow:     ['image/png', 'image/jpg', 'image/jpeg', 'image/gif', 'application/zip', 'audio/mpeg', 'text/csv', 'video/mp4', 'video/webm', 'application/pdf']
                        upload_deny:      ['all']
                        upload_max_size:  100M
        organigrammeStrategique:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Interne/Organigrammes/OrganigrammeStrategique
                        upload_allow:     ['image/png', 'image/jpg', 'image/jpeg', 'image/gif', 'application/zip', 'audio/mpeg', 'text/csv', 'video/mp4', 'video/webm', 'application/pdf']
                        upload_deny:      ['all']
                        upload_max_size:  100M
        organigrammeFonctionnel:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Interne/Organigrammes/OrganigrammeFonctionnel
                        upload_allow:     ['image/png', 'image/jpg', 'image/jpeg', 'image/gif', 'application/zip', 'audio/mpeg', 'text/csv', 'video/mp4', 'video/webm', 'application/pdf']
                        upload_deny:      ['all']
                        upload_max_size:  100M
        organigrammeAgence:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Interne/Organigrammes/OrganigrammeAgence
                        upload_allow:     ['image/png', 'image/jpg', 'image/jpeg', 'image/gif', 'application/zip', 'audio/mpeg', 'text/csv', 'video/mp4', 'video/webm', 'application/pdf']
                        upload_deny:      ['all']
                        upload_max_size:  100M
        annuaireAgences:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Interne/AnnuaireAgences
                        upload_allow:     ['image/png', 'image/jpg', 'image/jpeg', 'image/gif', 'application/zip', 'audio/mpeg', 'text/csv', 'video/mp4', 'video/webm', 'application/pdf']
                        upload_deny:      ['all']
                        upload_max_size:  100M
        CE:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Interne/CE
                        upload_allow:     ['image/png', 'image/jpg', 'image/jpeg', 'image/gif', 'application/zip', 'audio/mpeg', 'text/csv', 'video/mp4', 'video/webm', 'application/pdf']
                        upload_deny:      ['all']
                        upload_max_size:  100M
        CHSCT:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Interne/CHSCT
                        upload_allow:     ['image/png', 'image/jpg', 'image/jpeg', 'image/gif', 'application/zip', 'audio/mpeg', 'text/csv', 'video/mp4', 'video/webm', 'application/pdf']
                        upload_deny:      ['all']
                        upload_max_size:  100M
        noxALaConquete:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Marketing/NoxALaConquete
                        upload_allow:     ['image/png', 'image/jpg', 'image/jpeg', 'image/gif', 'application/zip', 'audio/mpeg', 'text/csv', 'video/mp4', 'video/webm', 'application/pdf']
                        upload_deny:      ['all']
                        upload_max_size:  100M
        ilYAForcementPlusSimple:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Marketing/IlYAForcementPlusSimple
                        upload_allow:     ['image/png', 'image/jpg', 'image/jpeg', 'image/gif', 'application/zip', 'audio/mpeg', 'text/csv', 'video/mp4', 'video/webm', 'application/pdf']
                        upload_deny:      ['all']
                        upload_max_size:  100M
        noxFournisseurDeTalents:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Marketing/NoxFournisseurDeTalents
                        upload_allow:     ['image/png', 'image/jpg', 'image/jpeg', 'image/gif', 'application/zip', 'audio/mpeg', 'text/csv', 'video/mp4', 'video/webm', 'application/pdf']
                        upload_deny:      ['all']
                        upload_max_size:  100M
        nos5Valeurs:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Marketing/Nos5Valeurs
                        upload_allow:     ['image/png', 'image/jpg', 'image/jpeg', 'image/gif', 'application/zip', 'audio/mpeg', 'text/csv', 'video/mp4', 'video/webm', 'application/pdf']
                        upload_deny:      ['all']
                        upload_max_size:  100M
        plaquettesInstitutionnelleLecture:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Externe/Supports/PlaquettesInstitutionnelle
                        attributes:
                            - { pattern: '/^[^\.].*/', read: true, write: false, locked: true }
                        upload_allow:
                        upload_deny:      ['all']
                        upload_max_size:  100M
        presentationPowerPointLecture:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Externe/Supports/PresentationPowerpoint
                        attributes:
                            - { pattern: '/^[^\.].*/', read: true, write: false, locked: true }
                        upload_allow:
                        upload_deny:      ['all']
                        upload_max_size:  100M
        fichesMetierLecture:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Externe/Supports/FichesMetier
                        attributes:
                            - { pattern: '/^[^\.].*/', read: true, write: false, locked: true }
                        upload_allow:
                        upload_deny:      ['all']
                        upload_max_size:  100M
        dossierTypeAppelOffreLecture:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Externe/Supports/DossierTypeAppelOffre
                        attributes:
                            - { pattern: '/^[^\.].*/', read: true, write: false, locked: true }
                        upload_allow:
                        upload_deny:      ['all']
                        upload_max_size:  100M
        CVTypeLecture:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Externe/Supports/CVType
                        attributes:
                            - { pattern: '/^[^\.].*/', read: true, write: false, locked: true }
                        upload_allow:
                        upload_deny:      ['all']
                        upload_max_size:  100M
        courriersTypeLecture:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Externe/Supports/CourriersType
                        attributes:
                            - { pattern: '/^[^\.].*/', read: true, write: false, locked: true }
                        upload_allow:
                        upload_deny:      ['all']
                        upload_max_size:  100M
        papierEnteteLecture:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Externe/Supports/PapierEntete
                        attributes:
                            - { pattern: '/^[^\.].*/', read: true, write: false, locked: true }
                        upload_allow:
                        upload_deny:      ['all']
                        upload_max_size:  100M
        noxDansLaPresseLecture:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Externe/NoxDansLaPresse
                        attributes:
                            - { pattern: '/^[^\.].*/', read: true, write: false, locked: true }
                        upload_allow:
                        upload_deny:      ['all']
                        upload_max_size:  100M
        logoNoxLecture:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Externe/BanqueImages/LogoNox
                        attributes:
                            - { pattern: '/^[^\.].*/', read: true, write: false, locked: true }
                        upload_allow:
                        upload_deny:      ['all']
                        upload_max_size:  100M
        cellesDuSiteInternetLecture:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Externe/BanqueImages/CellesDuSiteInternet
                        attributes:
                            - { pattern: '/^[^\.].*/', read: true, write: false, locked: true }
                        upload_allow:
                        upload_deny:      ['all']
                        upload_max_size:  100M
        cellesDesSupportsLecture:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Externe/BanqueImages/CellesDesSupports
                        attributes:
                            - { pattern: '/^[^\.].*/', read: true, write: false, locked: true }
                        upload_allow:
                        upload_deny:      ['all']
                        upload_max_size:  100M
        trombinoscopeLecture:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Interne/Trombinoscope
                        attributes:
                            - { pattern: '/^[^\.].*/', read: true, write: false, locked: true }
                        upload_allow:
                        upload_deny:      ['all']
                        upload_max_size:  100M
        organigrammeStrategiqueLecture:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Interne/Organigrammes/OrganigrammeStrategique
                        attributes:
                            - { pattern: '/^[^\.].*/', read: true, write: false, locked: true }
                        upload_allow:
                        upload_deny:      ['all']
                        upload_max_size:  100M
        organigrammeFonctionnelLecture:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Interne/Organigrammes/OrganigrammeFonctionnel
                        attributes:
                            - { pattern: '/^[^\.].*/', read: true, write: false, locked: true }
                        upload_allow:   ['all']
                        upload_deny:    
                        upload_max_size:  100M                
        organigrammeAgenceLecture:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Interne/Organigrammes/OrganigrammeAgence
                        attributes:
                            - { pattern: '/^[^\.].*/', read: true, write: false, locked: true }
                        upload_allow:
                        upload_deny:      ['all']
                        upload_max_size:  100M
        annuaireAgencesLecture:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Interne/AnnuaireAgences
                        attributes:
                            - { pattern: '/^[^\.].*/', read: true, write: false, locked: true }
                        upload_allow:
                        upload_allow:
                        upload_deny:      ['all']
                        upload_max_size:  100M
        CELecture:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Interne/CE
                        attributes:
                            - { pattern: '/^[^\.].*/', read: true, write: false, locked: true }
                        upload_allow:
                        upload_deny:      ['all']
                        upload_max_size:  100M
        CHSCTLecture:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Interne/CHSCT
                        attributes:
                            - { pattern: '/^[^\.].*/', read: true, write: false, locked: true }
                        upload_allow:
                        upload_deny:      ['all']
                        upload_max_size:  100M
        noxALaConqueteLecture:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Marketing/NoxALaConquete
                        attributes:
                            - { pattern: '/^[^\.].*/', read: true, write: false, locked: true }
                        upload_allow:
                        upload_deny:      ['all']
                        upload_max_size:  100M
        ilYAForcementPlusSimpleLecture:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Marketing/IlYAForcementPlusSimple
                        attributes:
                            - { pattern: '/^[^\.].*/', read: true, write: false, locked: true }
                        upload_allow:
                        upload_deny:      ['all']
                        upload_max_size:  100M
        noxFournisseurDeTalentsLecture:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Marketing/NoxFournisseurDeTalents
                        attributes:
                            - { pattern: '/^[^\.].*/', read: true, write: false, locked: true }
                        upload_allow:
                        upload_deny:      ['all']
                        upload_max_size:  100M
        nos5ValeursLecture:
            locale:           %locale%
            editor:           ckeditor
            fullscreen:       true
            include_assets:   true
            connector:
                debug:        true
                roots:
                    uploads:
                        driver:           LocalFileSystem
                        path:             uploads/Communication/Marketing/Nos5Valeurs
                        attributes:
                            - { pattern: '/^[^\.].*/', read: true, write: false, locked: true }
                        upload_allow:
                        upload_deny:      ['all']
                        upload_max_size:  100M
                        
ivory_ck_editor:
    default_config:    my_custom_config
    configs:
        my_custom_config:
            toolbar: [['NewPage', "-", 'DocProps', 'Preview']['Cut', 'Copy', 'Paste', '-', 'Undo', 'Redo'],'/',['Bold', 'Italic', 'Underline', 'Strike', '-', 'Subscript', 'Superscript', 'RemoveFormat'],['FontSize','TextColor'],['Image']]
            filebrowserBrowseRoute:     elfinder
            filebrowserBrowseRouteParameters:
                instance: ckeditor
